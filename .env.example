# No local environment variables are required for DB-free mode.
#
# The app uses:
# - /api/usace (proxy to USACE Dataquery)
# - /api/upstream-dams (upstream flow and trend data)
#
# Optional: add your own private vars here if you extend the app.
